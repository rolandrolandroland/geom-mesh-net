"""Experiment tracks. Run their scripts from the repository root as modules,
for example ``python -m experiments.inference.fit_posterior``.

    inference         what the global features can say about the parameters
    reconstruction    how well the guest-probability field can be recovered
    neural_field      the earlier coordinate-network experiments (E8, E9)
"""
